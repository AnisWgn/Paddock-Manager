<div class="form-card">
    {{ form_start(form) }}
        {{ form_widget(form) }}
        <div class="form-actions">
            <button type="submit" class="btn btn-f1">{{ button_label|default('Enregistrer') }}</button>
        </div>
    {{ form_end(form) }}
</div>
