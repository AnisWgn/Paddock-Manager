{% extends 'base.html.twig' %}

{% block title %}Modifier {{ plateforme.name }}{% endblock %}

{% block body %}
    <div class="page-header">
        <h1>Modifier {{ plateforme.name }}</h1>
    </div>

    {{ include('plateforme/_form.html.twig', {'button_label': 'Enregistrer'}) }}

    <div class="page-actions">
        <a href="{{ path('app_plateforme_index') }}" class="btn btn-outline">← Retour</a>
        {{ include('plateforme/_delete_form.html.twig') }}
    </div>
{% endblock %}
