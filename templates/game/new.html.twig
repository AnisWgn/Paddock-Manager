{% extends 'base.html.twig' %}

{% block title %}New Game{% endblock %}

{% block body %}
    <h1>Nouveau Grand Prix</h1>

    {{ include('game/_form.html.twig') }}

    <div class="page-actions">
        <a href="{{ path('app_game_index') }}" class="btn btn-outline">← Retour</a>
    </div>
{% endblock %}
