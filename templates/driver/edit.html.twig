{% extends 'base.html.twig' %}

{% block title %}Edit Driver{% endblock %}

{% block body %}
    <h1>Modifier {{ driver.firstName }} {{ driver.lastName }}</h1>

    {{ include('driver/_form.html.twig', {'button_label': 'Enregistrer'}) }}

    <div class="page-actions">
        <a href="{{ path('app_driver_index') }}" class="btn btn-outline">← Retour</a>
        {{ include('driver/_delete_form.html.twig') }}
    </div>
{% endblock %}
